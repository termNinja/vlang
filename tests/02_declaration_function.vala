// This tests function declarations and definitions.

/* Calculates square of number. */
int square(int x) {
    return x*x;
}

/* Calculates root of number. */
int root(int x);

int main() {
    return 0;
}
